import { TransportationFilterPipe } from './transportation-filter.pipe';

describe('TransportationFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TransportationFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
